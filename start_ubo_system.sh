#!/bin/bash
# UBO System Startup Script
# สคริปต์สำหรับเริ่มต้นระบบตรวจสอบ UBO

echo "🚀 กำลังเริ่มต้นระบบ UBO Detection System..."

# ตรวจสอบ Python
if ! command -v python3 &> /dev/null; then
    echo "❌ ไม่พบ Python3 กรุณาติดตั้ง Python3 ก่อน"
    exit 1
fi

# ตรวจสอบ pip
if ! command -v pip3 &> /dev/null; then
    echo "❌ ไม่พบ pip3 กรุณาติดตั้ง pip3 ก่อน"
    exit 1
fi

# ติดตั้ง dependencies
echo "📦 กำลังติดตั้ง dependencies..."
pip3 install -r requirements.txt

# ทดสอบระบบ
echo "🧪 กำลังทดสอบระบบกับบริษัท 0107548000234..."
python3 demo_ubo.py

# เริ่มต้น Web Application
echo "🌐 กำลังเริ่มต้น Web Application..."
echo "   เข้าถึงระบบได้ที่: http://localhost:5000"
echo "   กด Ctrl+C เพื่อหยุดระบบ"

python3 app.py
